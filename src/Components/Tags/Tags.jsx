const Tags = ({tag}) => {
       return (
              <div>
                     <p className="border-2 px-3 py-1 rounded-full text-sm">{tag}</p>
              </div>
       );
};

export default Tags;